<Window x:Class="WpfSEM02.Views.ReporteIngresosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reporte de Ingresos" Height="500" Width="750"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reporte de Ingresos" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Filtros de búsqueda -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Fecha Inicio:"/>
                <DatePicker x:Name="dpFechaInicio" Width="120"/>
            </StackPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Fecha Fin:"/>
                <DatePicker x:Name="dpFechaFin" Width="120"/>
            </StackPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Placa:"/>
                <TextBox x:Name="txtFiltroPlaca" Width="90"/>
            </StackPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Nombre Conductor:"/>
                <TextBox x:Name="txtFiltroConductor" Width="140"/>
            </StackPanel>
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Nombre Producto:"/>
                <TextBox x:Name="txtFiltroProducto" Width="120"/>
            </StackPanel>
            <Button x:Name="btnBuscar" Content="Buscar" Width="80" Height="25"
                    VerticalAlignment="Bottom" Click="btnBuscar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgIngresos" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaHora, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Placa" Binding="{Binding Placa}" Width="*"/>
                <DataGridTextColumn Header="Turno" Binding="{Binding Turno}" Width="*"/>
                <DataGridTextColumn Header="Conductor" Binding="{Binding NombreConductor}" Width="*"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*"/>
                <DataGridTextColumn Header="Peso" Binding="{Binding PesoIngreso}" Width="*"/>
                <DataGridTextColumn Header="Transporte" Binding="{Binding Transporte}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>