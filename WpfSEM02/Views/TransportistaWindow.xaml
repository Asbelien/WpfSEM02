<Window x:Class="WpfSEM02.Views.TransportistaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transportistas" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Transportistas" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Nombre:" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombre" Grid.Column="1" Margin="0,0,10,0"/>

            <TextBlock Text="RUC:" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox x:Name="txtRUC" Grid.Column="3" Margin="0,0,10,0"/>

            <TextBlock Text="Teléfono:" Grid.Column="4" VerticalAlignment="Center"/>
            <TextBox x:Name="txtTelefono" Grid.Column="5" Margin="0,0,10,0"/>

            <Button x:Name="btnGuardar" Content="Guardar" Grid.Column="6" Height="28" Click="btnGuardar_Click"/>
        </Grid>

        <DataGrid x:Name="dgTransportistas" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding NombreTransportista}" Width="*"/>
                <DataGridTextColumn Header="RUC" Binding="{Binding RUC}" Width="*"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>