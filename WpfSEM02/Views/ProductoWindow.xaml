<Window x:Class="WpfSEM02.Views.ProductoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Productos" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Productos" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Nombre Producto:" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombreProducto" Grid.Column="1" Margin="0,0,10,0"/>

            <TextBlock Text="Unidad:" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox x:Name="txtUnidad" Grid.Column="3" Margin="0,0,10,0"/>

            <Button x:Name="btnGuardar" Content="Guardar" Grid.Column="4" Height="28" Click="btnGuardar_Click"/>
        </Grid>

        <DataGrid x:Name="dgProductos" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre Producto" Binding="{Binding NombreProducto}" Width="*"/>
                <DataGridTextColumn Header="Unidad" Binding="{Binding Unidad}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>